using System;

namespace ALB
{
    enum Scene : uint
    {
        Car,
        Wheel,
        Line,
        House,
        Tree
    }
    enum PositionX : uint
    {
        Default,
        Left,
        Middle,
        Right
    }
    enum PositionY : uint
    {
        Default,
        Up,
        Middle,
        Down
    }
}
